
  # Event Review Campaign Layout (Copy)

  This is a code bundle for Event Review Campaign Layout (Copy). The original project is available at https://www.figma.com/design/JVru9cbqezJstTNGwld8Cp/Event-Review-Campaign-Layout--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  